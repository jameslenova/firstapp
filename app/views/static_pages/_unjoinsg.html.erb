<%= form_for(current_user.choosestudygroups.find_by_studygroup_id(sg),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "I Quit", class: "btn btn-large" %>
<% end %>